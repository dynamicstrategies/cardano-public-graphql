# Returns the Treasury Reserve, total fees for every epoch
# deposits + fees + reserves + rewards + treasury + utxo = 45bn ADA
# set the epoch_no to the most recent one

query Query {
    ada_pots {
        block_id
        deposits
        epoch_no
        fees
        id
        reserves
        rewards
        slot_no
        treasury
        utxo
    }
}